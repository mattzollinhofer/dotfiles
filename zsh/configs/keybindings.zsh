# https://zsh.sourceforge.io/Guide/zshguide04.html
# put Zsh Line Editor (zle) in emacs mode
# I thought I needed this, but maybe not if normal command line usage is working
# bindkey -e

# handy keybindings
bindkey "^R" history-incremental-search-backward

